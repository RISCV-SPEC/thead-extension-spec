[#xxtvcoder-insns-vabau-vv,reftext=Vector unsigned absolute difference and accumulate (vector-vector)]
==== xt.vabau.vv

Synopsis::
Vector unsigned absolute value of the difference and accumulate.

Mnemonic::
xt.vabau.vv _vd_, _vs2_, _vs1_, _vm_

Encoding::
[wavedrom, , svg]
....
{reg:[
    { bits:  7, name: 0xb, attr: ['custom-0, 32 bit'] },
    { bits:  5, name: 'vd' },
    { bits:  3, name: 0x6 },
    { bits:  5, name: 'vs1' },
    { bits:  5, name: 'vs2' },
    { bits:  1, name: 'vm' },
    { bits:  6, name: '0x2c' },
]}
....

Description::
Subtract each corresponding element of the two source registers and then compute the absolute value, accumulating to the destination registers. The operands are unsigned numbers.

Operation::
[source,sail]
--
vd[i] += abs(vs2[i] - vs1[i])
--

Permission::
This instruction can be executed in all privilege levels.

Exceptions::
This instruction does not trigger any exceptions.

Included in::
[%header]
|===
|Extension

|XXTVcoder (<<#xxtvcoder>>)
|===
